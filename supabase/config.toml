# Local config / Supabase CLI deploy
# Replace project_id after linking remote: supabase link

[api]
enabled = true

[functions.sellu-daily-sync]
# Scheduled invoke (cron / pg_net): no user JWT; protect with CRON_SECRET in the function
verify_jwt = false
